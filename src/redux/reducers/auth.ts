/* import { createReducer } from "@reduxjs/toolkit";

const authReducer = createReducer([], (builder)=> {
    builder
        .addCase('ADD_USER', (state, action) => {
            state = action.payload
        })
        .addCase('REMOVE_USER', (state)=>{
            state =  {}
        })
})

export default authReducer */